import { GermanWeekDayPipe } from './german-week-day.pipe';

describe('GermanWeekDayPipe', () => {
  it('create an instance', () => {
    const pipe = new GermanWeekDayPipe();
    expect(pipe).toBeTruthy();
  });
});
